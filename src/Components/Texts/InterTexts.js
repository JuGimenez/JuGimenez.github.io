import React from 'react'

import '../CContainer.css'

const ContainerInterTexts = ({ children }) => (
    <main className="inter-texts">
        {children}
    </main>
)

export default ContainerInterTexts