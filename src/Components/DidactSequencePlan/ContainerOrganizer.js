import React from 'react'

import '../CContainer.css'

const ContainerOrganizer = ({ children }) => (
    <main className="organizer">
        {children}
    </main>
)

export default ContainerOrganizer