import React from 'react'

import '../CContainer.css'

const ContainerHeaderDescription = ({ children }) => (
    <main className="header-description">
        {children}
    </main>
)

export default ContainerHeaderDescription