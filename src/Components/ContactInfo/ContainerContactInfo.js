import React from 'react'

import '../CContainer.css'

const ContainerContactInfo = ({ children }) => (
    <main className="contact-info">
        {children}
    </main>
)

export default ContainerContactInfo