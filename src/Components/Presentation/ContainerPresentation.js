import React from 'react'

import '../CContainer.css'

const ContainerPresentation = ({ children }) => (
    <main className="presentation">
        {children}
    </main>
)

export default ContainerPresentation