import React from 'react'

import '../CContainer.css'

const ContainerModalPdf = ({ children }) => (
    <main className="modal-pdf">
        {children}
    </main>
)

export default ContainerModalPdf