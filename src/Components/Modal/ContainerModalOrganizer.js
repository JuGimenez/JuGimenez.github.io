import React from 'react'

import '../CContainer.css'

const ContainerModalOrganizer = ({ children }) => (
    <main className="modal-organizer">
        {children}
    </main>
)

export default ContainerModalOrganizer