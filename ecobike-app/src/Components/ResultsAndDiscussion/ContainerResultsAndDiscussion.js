import React from 'react'

import '../CContainer.css'

const ContainerResultsAndDiscussion = ({ children }) => (
    <main className="results-and-discussion">
        {children}
    </main>
)

export default ContainerResultsAndDiscussion