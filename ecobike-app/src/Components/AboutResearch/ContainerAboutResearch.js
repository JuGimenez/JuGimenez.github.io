import React from 'react'

import '../CContainer.css'

const ContainerAboutResearch = ({ children }) => (
    <main className="about-research">
        {children}
    </main>
)

export default ContainerAboutResearch