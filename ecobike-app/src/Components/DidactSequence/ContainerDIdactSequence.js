import React from 'react'

import '../CContainer.css'

const ContainerDidactSequence = ({ children }) => (
    <main className="didact-sequence">
        {children}
    </main>
)

export default ContainerDidactSequence