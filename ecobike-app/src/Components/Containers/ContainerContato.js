import React from 'react'

import './CContainer.css'

const ContainerContato = ({ children }) => (
    <main className="container-contato">
        {children}
    </main>
)

export default ContainerContato