import React from 'react'

import '../CContainer.css'

const ContainerGalleryShowcase = ({ children }) => (
    <main className="gallery-showcase">
        {children}
    </main>
)

export default ContainerGalleryShowcase